%h2 新しく地域Ruby会議を開催する

- form_for(@event, :url => {:action => "create"}) do |f|
  - if @event.errors.any?
    <ul>
    - @event.errors.full_messages.each do |msg|
      %li= msg
  = render :partial => "form",:locals => {:form => f}
  %p= f.submit "Create"

= render :partial => "preview"

.backto= link_to 'イベント一覧', admin_events_path
